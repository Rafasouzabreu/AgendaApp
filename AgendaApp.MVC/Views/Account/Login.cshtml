@model AgendaApp.MVC.Models.Account.AccountLoginViewModel

@{
    Layout = "~/Views/Shared/_AccountLayout.cshtml";
}

<div class="row">
    <div class="col-md-4 offset-md-4">
        <div class="card">
            <div class="card-body">
                <div class="text-center">
                    <img src="https://www.cotiinformatica.com.br/imagens/logo-coti-informatica.svg" />
                    <h4>Agenda de Tarefas</h4>
                    <p>Acesso ao sistema</p>
                </div>

                @using (Html.BeginForm())
                {
                    <div class="mb-3">
                        <label>Email de acesso:</label>
                        @Html.TextBoxFor(model => model.Email, new { @class = "form-control" })
                    </div>

                    <div class="mb-3">
                        <label>Senha de acesso:</label>
                        @Html.PasswordFor(model => model.Senha, new { @class = "form-control" })
                    </div>

                    <div class="mb-3 d-grid">
                        <input type="submit" class="btn btn-primary btn-sm" value="Acessar Conta" />
                    </div>

                    <div class="mb-3 d-grid">
                        <a href="/Account/Register" class="btn btn-light btn-sm">
                            Não possui conta? <strong>Cadastre-se aqui!</strong>
                        </a>
                    </div>
                }

            </div>
        </div>
    </div>
</div>


